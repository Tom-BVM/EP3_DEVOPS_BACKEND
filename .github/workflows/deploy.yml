name: Deploy Backend

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-session-token: ${{ secrets.AWS_SESSION_TOKEN }}
          aws-region: us-east-1

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v1

      - name: Build, tag, and push image
        run: |
          docker build -t ep3-backend .
          docker tag ep3-backend:latest 585541687273.dkr.ecr.us-east-1.amazonaws.com/ep3-backend:latest
          docker push 585541687273.dkr.ecr.us-east-1.amazonaws.com/ep3-backend:latest

      - name: Deploy to ECS
        run: |
          aws ecs update-service --cluster ep3-cluster --service ep3-backend-service --force-new-deployment
